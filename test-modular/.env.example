# Arquivo .env de teste - use as mesmas configurações do .env principal
NODE_ENV=development
PORT=4000
JWT_SECRET=seu_jwt_secret_aqui

# Database (mesmas configurações do projeto principal)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=seu_database_name
DB_USER=seu_database_user
DB_PASSWORD=sua_database_password
DB_SSL=false
